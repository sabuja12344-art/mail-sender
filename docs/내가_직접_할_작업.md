# 단계별 작업 — 복사 가능 버전

아래 **1단계 → 2단계 → …** 순서대로 진행하세요. **📋 복사** 표시된 블록은 그대로 드래그해서 복사해 쓰면 됩니다.

---

# 1단계: Supabase DB 준비

## 1-1. Supabase 접속

- 브라우저에서 아래 주소로 이동 후 로그인, 사용 중인 프로젝트 클릭.

**📋 복사 — Supabase 대시보드 주소**

```
https://supabase.com/dashboard
```

---

## 1-2. SQL Editor 열기

- 왼쪽 메뉴 **SQL Editor** 클릭 → **+ New query** 로 새 쿼리 창 열기.

---

## 1-3. brands 테이블 생성 — 아래 SQL 전체 복사 후 SQL Editor에 붙여넣고 Run

**📋 복사 — 1-3 (brands 테이블 생성 SQL 전체)**

```sql
-- brands 테이블: 브랜드 정보 및 발송 상태 관리
-- Supabase 데이터베이스 스키마

-- enum: status 값 제한 (선택 사항, 일관성 보장)
CREATE TYPE brand_status AS ENUM (
  '수집완료',
  '제안생성',
  '발송대기',
  '발송완료',
  '회신완료'
);

-- brands 테이블 생성
CREATE TABLE public.brands (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name TEXT NOT NULL,
  website_url TEXT,
  email TEXT,
  pixel_installed BOOLEAN NOT NULL DEFAULT false,
  analysis_summary TEXT,
  status brand_status NOT NULL DEFAULT '수집완료',
  created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- 인덱스: 상태별 조회, 생성일 정렬
CREATE INDEX idx_brands_status ON public.brands (status);
CREATE INDEX idx_brands_created_at ON public.brands (created_at DESC);

-- RLS(Row Level Security) 활성화 (Supabase 권장)
ALTER TABLE public.brands ENABLE ROW LEVEL SECURITY;

-- 정책 예시: 인증된 사용자만 모든 작업 허용 (실제 권한은 프로젝트에 맞게 수정)
CREATE POLICY "Allow authenticated users full access to brands"
  ON public.brands
  FOR ALL
  TO authenticated
  USING (true)
  WITH CHECK (true);

-- 서비스 역할(백엔드) 전체 접근 정책 (필요 시)
CREATE POLICY "Allow service role full access to brands"
  ON public.brands
  FOR ALL
  TO service_role
  USING (true)
  WITH CHECK (true);

-- 코멘트
COMMENT ON TABLE public.brands IS '수집된 브랜드 정보와 발송 상태 관리';
COMMENT ON COLUMN public.brands.name IS '브랜드명';
COMMENT ON COLUMN public.brands.website_url IS '웹사이트 주소';
COMMENT ON COLUMN public.brands.email IS '추출된 대표 이메일';
COMMENT ON COLUMN public.brands.pixel_installed IS '픽셀 설치 여부';
COMMENT ON COLUMN public.brands.analysis_summary IS 'AI가 분석한 브랜드 약점/특징';
COMMENT ON COLUMN public.brands.status IS '상태: 수집완료 / 제안생성 / 발송대기 / 발송완료 / 회신완료';
COMMENT ON COLUMN public.brands.created_at IS '수집 일자';
```

---

## 1-4. '확인완료' 상태 추가 — 아래 한 줄만 복사해 새 쿼리에 붙여넣고 Run

**📋 복사 — 1-4 (확인완료 추가 SQL)**

```sql
ALTER TYPE brand_status ADD VALUE '확인완료';
```

---

**1단계 완료 확인:** 왼쪽 메뉴 **Table Editor** → **brands** 테이블이 보이면 성공.

---

# 2단계: API 키·시크릿 발급 및 등록

## 2-1. Gemini API 키 받기

- 아래 주소 접속 → 로그인 → **Create API Key** → 키 **복사**해 두기.

**📋 복사 — Google AI Studio 주소**

```
https://aistudio.google.com/apikey
```

---

## 2-2. Resend API 키 받기

- 아래 주소 접속 → 가입/로그인 → **API Keys** → **Create API Key** → 키(re_ 로 시작) **복사**.

**📋 복사 — Resend 주소**

```
https://resend.com
```

- 발신 이메일(테스트용): 아래 주소 그대로 사용 가능.

**📋 복사 — 테스트용 발신 이메일**

```
onboarding@resend.dev
```

---

## 2-3. Supabase Secrets에 등록

- Supabase 대시보드 → **Project Settings(톱니바퀴)** → **Edge Functions** → **Secrets**.
- **Add new secret** 을 3번 반복해서 아래 이름·값을 넣기.

| Name (이름) | Value (값) |
|-------------|------------|
| `GEMINI_API_KEY` | 2-1에서 복사한 Gemini 키 |
| `RESEND_API_KEY` | 2-2에서 복사한 Resend 키 (re_…) |
| `RESEND_FROM_EMAIL` | `onboarding@resend.dev` 또는 본인 발신 이메일 |

**📋 복사 — Secret 이름 (한 줄씩)**

```
GEMINI_API_KEY
```

```
RESEND_API_KEY
```

```
RESEND_FROM_EMAIL
```

**📋 복사 — Value 예시 (테스트용 발신 이메일)**

```
onboarding@resend.dev
```

---

**2단계 완료 확인:** Secrets 목록에 위 3개가 보이면 성공.

---

# 3단계: Edge Function 배포

## 3-1. 터미널 열고 프로젝트 폴더로 이동

- **Win + R** → `cmd` 또는 `powershell` 입력 → 엔터.
- 아래를 **한 줄씩** 입력 후 엔터 (경로가 다르면 본인 경로로 수정).

**📋 복사 — 3-1 프로젝트 폴더로 이동**

```
cd "c:\Users\PC\Desktop\영업 메일링"
```

---

## 3-2. Supabase 로그인

**📋 복사 — 3-2 로그인**

```
npx supabase login
```

- 브라우저가 열리면 로그인.

---

## 3-3. 프로젝트 연결

**📋 복사 — 3-3 링크 (project-ref)**

```
npx supabase link --project-ref rchflauwrrlfefxzbtks
```

- DB 비밀번호 물어보면: Supabase **Settings → Database** 에서 확인한 비밀번호 입력.

---

## 3-4. Edge Function 3개 배포 — 아래 명령을 한 줄씩 실행

**📋 복사 — 3-4-1**

```
npx supabase functions deploy generate-proposals
```

**📋 복사 — 3-4-2**

```
npx supabase functions deploy send-proposal-emails
```

**📋 복사 — 3-4-3**

```
npx supabase functions deploy proposal-viewed
```

---

**3단계 완료 확인:** Supabase **Edge Functions** 메뉴에 위 3개 함수가 보이면 성공.

---

# 4단계: 브랜드 수집 (크롤러)

## 4-1. 프로젝트 폴더로 이동

**📋 복사 — 4-1**

```
cd "c:\Users\PC\Desktop\영업 메일링"
```

---

## 4-2. 처음 한 번만 — 패키지 설치

**📋 복사 — 4-2-1**

```
pip install -r requirements.txt
```

**📋 복사 — 4-2-2**

```
playwright install chromium
```

---

## 4-3. 크롤러 실행 (키워드 없으면 "자사몰" 검색)

**📋 복사 — 4-3 기본 실행**

```
python crawler/brand_crawler.py
```

**📋 복사 — 4-3 키워드 지정 예시**

```
python crawler/brand_crawler.py 여성의류
```

---

**4단계 완료 확인:** Supabase **Table Editor → brands** 에 새 행이 추가되고 status 가 "수집완료" 이면 성공.

---

# 5단계: 운영 대시보드 로컬 실행

## 5-1. 서비스 롤 키 복사

- Supabase **Settings → API** → **service_role** 옆 **Reveal** 클릭 → 값 **복사**해 두기.

---

## 5-2. .env.local 파일 만들기

- **영업 메일링** 폴더 안 **web** 폴더에 **`.env.local`** 파일 생성.
- 아래 두 줄을 넣고, **두 번째 줄의 `여기에_서비스_롤_키_붙여넣기`** 를 5-1에서 복사한 값으로 **통째로** 바꾼 뒤 저장.

**📋 복사 — 5-2 .env.local 내용**

```
SUPABASE_URL=https://rchflauwrrlfefxzbtks.supabase.co
SUPABASE_SERVICE_ROLE_KEY=여기에_서비스_롤_키_붙여넣기
```

---

## 5-3. 대시보드 실행 — 아래 명령을 한 줄씩 실행

**📋 복사 — 5-3-1**

```
cd "c:\Users\PC\Desktop\영업 메일링\web"
```

**📋 복사 — 5-3-2**

```
npm install
```

**📋 복사 — 5-3-3**

```
npm run dev
```

---

## 5-4. 브라우저에서 접속

**📋 복사 — 대시보드 주소**

```
http://localhost:3000
```

- 주소창에 붙여넣기 후 엔터 → 브랜드 목록·버튼이 보이면 5단계 완료.

---

# 6단계: 평소 운영 (반복)

## 6-1. AI 제안 생성

- 브라우저에서 `http://localhost:3000` 접속 → **「AI 제안 생성」** 버튼 클릭.

## 6-2. 발송 대상 지정

- Supabase **Table Editor → brands** → 메일 보낼 행의 **status** 를 **발송대기** 로 변경.

## 6-3. 메일 발송

- 대시보드에서 **「메일 발송」** 버튼 클릭.

---

# 7단계 (선택): Vercel 배포 — 인터넷 URL로 대시보드 쓰기

## 7-1. Vercel 접속

**📋 복사 — Vercel 주소**

```
https://vercel.com
```

- 가입/로그인 후 **Add New… → Project**.

## 7-2. Root Directory

- **Root Directory** 를 **`web`** 으로 지정.

## 7-3. 환경 변수 — 아래 이름·값을 Vercel Environment Variables에 추가

**📋 복사 — 7-3 Name 1**

```
SUPABASE_URL
```

**📋 복사 — 7-3 Value 1**

```
https://rchflauwrrlfefxzbtks.supabase.co
```

**📋 복사 — 7-3 Name 2**

```
SUPABASE_SERVICE_ROLE_KEY
```

**📋 복사 — 7-3 Value 2**  
→ 5단계에서 쓴 **서비스 롤 키**를 그대로 붙여넣기.

## 7-4. Deploy

- **Deploy** 클릭 → 완료 후 나오는 URL(예: `https://xxx.vercel.app`)로 접속.

---

# 복사용 한눈에 정리

| 용도 | 복사할 내용 |
|------|-------------|
| 1-3 SQL 전체 | 위 **📋 복사 — 1-3** 코드 블록 |
| 1-4 SQL 한 줄 | `ALTER TYPE brand_status ADD VALUE '확인완료';` |
| 프로젝트 폴더 이동 | `cd "c:\Users\PC\Desktop\영업 메일링"` |
| web 폴더 이동 | `cd "c:\Users\PC\Desktop\영업 메일링\web"` |
| Supabase 링크 | `npx supabase link --project-ref rchflauwrrlfefxzbtks` |
| deploy 3개 | `generate-proposals`, `send-proposal-emails`, `proposal-viewed` 각각 deploy 명령 |
| .env.local | `SUPABASE_URL=...` / `SUPABASE_SERVICE_ROLE_KEY=...` 두 줄 |
| 대시보드 주소 | `http://localhost:3000` |

위 순서대로 **1단계부터** 진행하고, 📋 표시된 블록만 복사해서 사용하면 됩니다.
