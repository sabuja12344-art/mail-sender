# 진행 상황 점검 + 안 되어 있는 부분만 할 작업

---

# 1. 지금까지 작업 검토 결과

## 1-1. 프로젝트에서 확인된 것 (자동 점검)

| 항목 | 상태 | 비고 |
|------|------|------|
| 프로젝트 폴더·코드 | ✅ 있음 | crawler, web, supabase 함수·마이그레이션 파일 존재 |
| 루트 `.env` | ✅ 수정 완료 | `SUPABASE_URL` 올바른 ref 로 반영됨 |
| `web/.env.local` | ❌ 없음 | 대시보드용 환경 변수 파일이 없음 → **5단계에서 만들어야 함** |
| Supabase 링크 | ✅ 됨 | `supabase link` 로 `rchflauwrrlfefxzbtks` 프로젝트 연결된 상태 |

## 1-2. .env URL (수정 완료 시 생략)

`SUPABASE_URL` 이 `https://rchflauwrrlfefxzbtks.supabase.co` 로 되어 있으면 OK.

---

## 1-3. 본인이 직접 확인해야 할 것 (대시보드·실행)

아래는 파일만으로는 알 수 없으니, **본인이 한 번씩 확인**해 주세요.

| 단계 | 확인 방법 | 완료했으면 ✓ |
|------|-----------|----------------|
| **1단계 DB** | Supabase **Table Editor** 에서 **brands** 테이블이 보이는지 | □ |
| | **brands** 가 있을 때, SQL Editor에서 `SELECT unnest(enum_range(NULL::brand_status));` 실행 시 **확인완료** 가 목록에 있는지 | □ |
| **2단계 Secrets** | Supabase **Settings → Edge Functions → Secrets** 에 **GEMINI_API_KEY**, **RESEND_API_KEY**, **RESEND_FROM_EMAIL** 이 있는지 | □ |
| **3단계 배포** | **Edge Functions** 메뉴에 **generate-proposals**, **send-proposal-emails**, **proposal-viewed** 3개가 있는지 | □ |
| **4단계 크롤러** | **Table Editor → brands** 에 수집된 행이 1개 이상 있는지 (직접 수집했을 때만) | □ |
| **5단계 대시보드** | `cd web` → `npm install` → `npm run dev` 후 **http://localhost:3000** 접속 시 화면이 뜨는지 | □ |

위 표에서 **✓ 안 한 것**이 곧 “아직 안 된 부분”입니다.  
아래 **2. 안 되어 있는 부분만 — 내가 할 작업** 에서 해당 번호만 골라서 진행하면 됩니다.

---

# 2. 안 되어 있는 부분만 — 내가 할 작업 (상세)

아래는 **1-3에서 체크하지 않은(미완료) 단계만** 골라서 하면 됩니다.  
이미 완료한 단계는 건너뛰세요.

---

## □ 1단계가 안 되어 있을 때: Supabase DB 준비

### 1-1. Supabase 접속

**📋 복사 — 대시보드 주소**

```
https://supabase.com/dashboard
```

로그인 후 사용 중인 프로젝트 클릭.

### 1-2. SQL Editor 열기

왼쪽 **SQL Editor** → **+ New query**.

### 1-3. brands 테이블 생성

아래 SQL **전체**를 복사해 SQL Editor에 붙여넣고 **Run** 클릭.

**📋 복사 — 1단계 SQL 전체 (brands 테이블)**

```sql
-- brands 테이블: 브랜드 정보 및 발송 상태 관리
CREATE TYPE brand_status AS ENUM (
  '수집완료',
  '제안생성',
  '발송대기',
  '발송완료',
  '회신완료'
);

CREATE TABLE public.brands (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name TEXT NOT NULL,
  website_url TEXT,
  email TEXT,
  pixel_installed BOOLEAN NOT NULL DEFAULT false,
  analysis_summary TEXT,
  status brand_status NOT NULL DEFAULT '수집완료',
  created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX idx_brands_status ON public.brands (status);
CREATE INDEX idx_brands_created_at ON public.brands (created_at DESC);

ALTER TABLE public.brands ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Allow authenticated users full access to brands"
  ON public.brands FOR ALL TO authenticated USING (true) WITH CHECK (true);

CREATE POLICY "Allow service role full access to brands"
  ON public.brands FOR ALL TO service_role USING (true) WITH CHECK (true);

COMMENT ON TABLE public.brands IS '수집된 브랜드 정보와 발송 상태 관리';
COMMENT ON COLUMN public.brands.name IS '브랜드명';
COMMENT ON COLUMN public.brands.website_url IS '웹사이트 주소';
COMMENT ON COLUMN public.brands.email IS '추출된 대표 이메일';
COMMENT ON COLUMN public.brands.pixel_installed IS '픽셀 설치 여부';
COMMENT ON COLUMN public.brands.analysis_summary IS 'AI가 분석한 브랜드 약점/특징';
COMMENT ON COLUMN public.brands.status IS '상태';
COMMENT ON COLUMN public.brands.created_at IS '수집 일자';
```

### 1-4. '확인완료' 추가

에디터 내용 **지우고** 아래 **한 줄만** 붙여넣은 뒤 다시 **Run**.

**📋 복사 — 확인완료 추가**

```sql
ALTER TYPE brand_status ADD VALUE '확인완료';
```

**완료 확인:** Table Editor에 **brands** 테이블이 보이면 1단계 끝.

---

## □ 2단계가 안 되어 있을 때: API 키·Secrets 등록

### 2-1. Gemini API 키

**📋 복사 — Google AI Studio**

```
https://aistudio.google.com/apikey
```

접속 → **Create API Key** → 키 **복사**.

### 2-2. Resend API 키

**📋 복사 — Resend**

```
https://resend.com
```

가입/로그인 → **API Keys** → **Create API Key** → 키(re_…) **복사**.

### 2-3. Supabase에 Secrets 넣기

Supabase **Settings(톱니바퀴)** → **Edge Functions** → **Secrets** → **Add new secret** 3번:

| Name (정확히 이렇게) | Value |
|---------------------|--------|
| `GEMINI_API_KEY` | 2-1에서 복사한 키 |
| `RESEND_API_KEY` | 2-2에서 복사한 키 |
| `RESEND_FROM_EMAIL` | `onboarding@resend.dev` 또는 본인 발신 이메일 |

**📋 복사 — Secret 이름**

```
GEMINI_API_KEY
```

```
RESEND_API_KEY
```

```
RESEND_FROM_EMAIL
```

**📋 복사 — 테스트용 발신 이메일**

```
onboarding@resend.dev
```

**완료 확인:** Secrets 목록에 위 3개가 보이면 2단계 끝.

---

## □ 3단계가 안 되어 있을 때: Edge Function 배포

터미널(명령 프롬프트 또는 PowerShell)을 열고 아래를 **한 줄씩** 실행.

**📋 복사 — 프로젝트 폴더 이동**

```
cd "c:\Users\PC\Desktop\영업 메일링"
```

**📋 복사 — 로그인**

```
npx supabase login
```

브라우저에서 로그인.

**📋 복사 — 프로젝트 연결**

```
npx supabase link --project-ref rchflauwrrlfefxzbtks
```

DB 비밀번호 물어보면 Supabase **Settings → Database** 에서 확인.

**📋 복사 — 배포 1**

```
npx supabase functions deploy generate-proposals
```

**📋 복사 — 배포 2**

```
npx supabase functions deploy send-proposal-emails
```

**📋 복사 — 배포 3**

```
npx supabase functions deploy proposal-viewed
```

**완료 확인:** Supabase **Edge Functions** 메뉴에 위 3개가 보이면 3단계 끝.

---

## □ 4단계가 안 되어 있을 때: 브랜드 수집 (크롤러)

**📋 복사 — 프로젝트 폴더**

```
cd "c:\Users\PC\Desktop\영업 메일링"
```

**처음 한 번만:**

**📋 복사 — 패키지 설치**

```
pip install -r requirements.txt
```

```
playwright install chromium
```

**크롤러 실행:**

**📋 복사 — 크롤러 실행**

```
python crawler/brand_crawler.py
```

키워드 지정 예:

```
python crawler/brand_crawler.py 여성의류
```

**완료 확인:** Table Editor → **brands** 에 새 행이 생기고 status 가 "수집완료" 이면 4단계 끝.

---

## □ 5단계가 안 되어 있을 때: 운영 대시보드 실행

### 5-1. 서비스 롤 키 복사

Supabase **Settings → API** → **service_role** 옆 **Reveal** → 값 **복사**.

### 5-2. .env.local 만들기

1. **영업 메일링** 폴더 안 **web** 폴더로 이동.
2. **web** 폴더 안에 새 파일 **`.env.local`** 생성 (메모장 등).
3. 아래 두 줄을 넣고, **두 번째 줄**의 `여기_서비스_롤_키` 를 5-1에서 복사한 값으로 **통째로** 바꾼 뒤 저장.

**📋 복사 — .env.local 내용**

```
SUPABASE_URL=https://rchflauwrrlfefxzbtks.supabase.co
SUPABASE_SERVICE_ROLE_KEY=여기_서비스_롤_키
```

### 5-3. 대시보드 실행

**📋 복사 — web 폴더 이동**

```
cd "c:\Users\PC\Desktop\영업 메일링\web"
```

**📋 복사 — npm 설치**

```
npm install
```

**📋 복사 — 개발 서버 실행**

```
npm run dev
```

### 5-4. 브라우저 접속

**📋 복사 — 대시보드 주소**

```
http://localhost:3000
```

주소창에 붙여넣기 → 브랜드 목록·버튼이 보이면 5단계 끝.

---

# 3. 정리: 지금 당장 할 일만

1. ~~**.env 수정**~~ → 완료했다면 생략.

2. **1-3 점검 표** 에서 □ 인 단계만 골라서, 위 **2. 안 되어 있는 부분만** 의 해당 블록만 진행.

3. 모두 ✓ 되면:  
   대시보드(localhost:3000)에서 **AI 제안 생성** → Table Editor에서 **발송대기** 설정 → **메일 발송** 순서로 운영하면 됩니다.

추가로 필요한 게 있으면 말해 주세요.
