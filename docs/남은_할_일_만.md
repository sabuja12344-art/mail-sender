# 지금까지 한 작업 점검 + 남은 할 일만

프로젝트를 기준으로 확인한 결과와, **안 한 것만** 정리했습니다. 이미 한 항목은 건너뛰세요.

---

# 1. 이미 된 것 (확인됨)

아래는 **파일/설정 기준으로 이미 되어 있는 것**입니다. 다시 할 필요 없습니다.

| 항목 | 상태 |
|------|------|
| 루트 `.env` | ✅ 있음. `SUPABASE_URL` 올바른 ref (`rchflauwrrlfefxzbtks`) 로 되어 있음 |
| Supabase 프로젝트 연결 | ✅ `supabase link` 로 `rchflauwrrlfefxzbtks` 연결된 상태 |

---

# 2. 본인이 이미 했는지만 확인 (했으면 할 일에서 제외)

아래는 **이미 했다면** 더 이상 하지 않아도 됩니다. **안 했다면**만 3번으로 가서 진행하세요.

- **1단계 DB:** Supabase **Table Editor**에 **brands** 테이블이 보인다 → ✅ 했다면 1단계 건너뛰기
- **2단계 Secrets:** Supabase **Settings → Edge Functions → Secrets**에 **GEMINI_API_KEY**, **RESEND_API_KEY**, **RESEND_FROM_EMAIL** 3개가 있다 → ✅ 했다면 2단계 건너뛰기
- **3단계 배포:** Supabase **Edge Functions** 메뉴에 **generate-proposals**, **send-proposal-emails**, **proposal-viewed** 3개가 있다 → ✅ 했다면 3단계 건너뛰기
- **4단계 크롤러:** 브랜드 수집이 필요 없거나, 이미 **brands** 테이블에 데이터가 있다 → ✅ 했다면 4단계 건너뛰기

---

# 3. 남은 할 일만 (안 한 것만 하세요)

아래는 **아직 안 한 것만** 골라서 하면 됩니다. 이미 했으면 해당 블록 통째로 건너뛰세요.

---

## □ 1단계를 아직 안 했을 때만: DB (brands 테이블 + 확인완료)

- **어디:** Supabase 대시보드 → **SQL Editor** → **+ New query**
- **액션:** 아래 SQL **전체** 복사해 붙여넣기 → **Run**  
  그다음 에디터 **비우고** 맨 아래 한 줄만 붙여넣기 → **Run**

```sql
CREATE TYPE brand_status AS ENUM (
  '수집완료',
  '제안생성',
  '발송대기',
  '발송완료',
  '회신완료'
);

CREATE TABLE public.brands (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name TEXT NOT NULL,
  website_url TEXT,
  email TEXT,
  pixel_installed BOOLEAN NOT NULL DEFAULT false,
  analysis_summary TEXT,
  status brand_status NOT NULL DEFAULT '수집완료',
  created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX idx_brands_status ON public.brands (status);
CREATE INDEX idx_brands_created_at ON public.brands (created_at DESC);

ALTER TABLE public.brands ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Allow authenticated users full access to brands"
  ON public.brands FOR ALL TO authenticated USING (true) WITH CHECK (true);

CREATE POLICY "Allow service role full access to brands"
  ON public.brands FOR ALL TO service_role USING (true) WITH CHECK (true);

COMMENT ON TABLE public.brands IS '수집된 브랜드 정보와 발송 상태 관리';
COMMENT ON COLUMN public.brands.name IS '브랜드명';
COMMENT ON COLUMN public.brands.website_url IS '웹사이트 주소';
COMMENT ON COLUMN public.brands.email IS '추출된 대표 이메일';
COMMENT ON COLUMN public.brands.pixel_installed IS '픽셀 설치 여부';
COMMENT ON COLUMN public.brands.analysis_summary IS 'AI가 분석한 브랜드 약점/특징';
COMMENT ON COLUMN public.brands.status IS '상태';
COMMENT ON COLUMN public.brands.created_at IS '수집 일자';
```

```sql
ALTER TYPE brand_status ADD VALUE '확인완료';
```

---

## □ 2단계를 아직 안 했을 때만: Secrets (API 키 3개)

- **어디:** Supabase **Settings** → **Edge Functions** → **Secrets** → **Add new secret**
- **액션:** 아래 3개를 **한 번씩** Name / Value 넣고 **Save**
  - Name: `GEMINI_API_KEY` / Value: [Google AI Studio](https://aistudio.google.com/apikey)에서 발급한 키
  - Name: `RESEND_API_KEY` / Value: [Resend](https://resend.com) API Keys에서 발급한 키 (re_…)
  - Name: `RESEND_FROM_EMAIL` / Value: `onboarding@resend.dev` 또는 본인 발신 이메일

---

## □ 3단계를 아직 안 했을 때만: Edge Function 배포

- **어디:** 터미널(명령 프롬프트 또는 PowerShell)
- **액션:** 아래를 **한 줄씩** 붙여넣고 엔터 (이미 로그인·링크 했다면 `cd` 다음 세 줄만 해도 됨)

```
cd "c:\Users\PC\Desktop\영업 메일링"
```

```
npx supabase functions deploy generate-proposals
```

```
npx supabase functions deploy send-proposal-emails
```

```
npx supabase functions deploy proposal-viewed
```

---

## □ 4단계를 아직 안 했을 때만: 브랜드 수집 (크롤러)

- **어디:** 터미널
- **액션:** 아래를 순서대로 실행 (처음이면 1·2 먼저, 이미 했으면 3만)

```
cd "c:\Users\PC\Desktop\영업 메일링"
```

```
pip install -r requirements.txt
```

```
playwright install chromium
```

```
python crawler/brand_crawler.py
```

---

## ■ 반드시 할 일: 대시보드용 .env.local + 실행

**현재 프로젝트에 `web/.env.local` 이 없어서, 아래는 꼭 해야 합니다.**

### (1) 서비스 롤 키 복사

- **어디:** Supabase **Settings** → **API**
- **액션:** **service_role** 옆 **Reveal** 클릭 → 값 **전체 복사**

### (2) .env.local 만들기

- **어디:** **영업 메일링** 폴더 안 **web** 폴더
- **액션:**  
  - 새 파일 만들기 → 이름: **`.env.local`**  
  - 안에 아래 두 줄 넣기 (두 번째 줄은 방금 복사한 서비스 롤 키로 **통째로** 바꾸기)  
  - 저장

```
SUPABASE_URL=https://rchflauwrrlfefxzbtks.supabase.co
SUPABASE_SERVICE_ROLE_KEY=여기_서비스_롤_키_붙여넣기
```

### (3) 대시보드 실행

- **어디:** 터미널
- **액션:** 아래를 **한 줄씩** 실행 (터미널 창은 닫지 말기)

```
cd "c:\Users\PC\Desktop\영업 메일링\web"
```

```
npm install
```

```
npm run dev
```

- **어디:** 브라우저 주소창
- **액션:** `http://localhost:3000` 입력 후 엔터 → 화면 뜨면 완료

---

# 4. 한눈에 정리

| 항목 | 해야 함? |
|------|----------|
| 1단계 DB (brands + 확인완료) | Table Editor에 brands 없으면 **한다** |
| 2단계 Secrets 3개 | Secrets에 3개 없으면 **한다** |
| 3단계 Edge Function 배포 | Edge Functions에 3개 없으면 **한다** |
| 4단계 크롤러 | 브랜드 데이터 필요하고 아직 수집 안 했으면 **한다** |
| **.env.local 만들기 + npm run dev** | **항상 한다** (현재 .env.local 없음) |

이미 한 작업은 빼고, 위 표에서 “한다”인 것만 하시면 됩니다.
